{{/*
# Copyright 2024-present Intel Corporation
# Copyright 2020-present Open Networking Foundation

# SPDX-License-Identifier: Apache-2.0
*/}}
{{- if .Values.config.webui.deploy }}
{{ tuple "webui" . | include "5g-control-plane.service_account" }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webui
  labels:
{{ tuple "webui" . | include "5g-control-plane.metadata_labels" | indent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
{{ tuple "webui" . | include "5g-control-plane.metadata_labels" | indent 6 }}
  template:
    metadata:
      labels:
{{ tuple "webui" . | include "5g-control-plane.metadata_labels" | indent 8 }}
      annotations:
    {{- tuple "webui" . | include "5g-control-plane.certs_pod_annotations" | nindent 8 }}
    {{- with .Values.config.webui.podAnnotations }}
    {{- toYaml . | nindent 8 }}
    {{- end }}
    spec:
      serviceAccountName: webui
    {{- if .Values.nodeSelectors.enabled }}
      nodeSelector:
        {{ .Values.nodeSelectors.label }}: {{ .Values.nodeSelectors.value | quote }}
    {{- end }}
    {{- if hasKey .Values.images "pullSecrets" }}
      imagePullSecrets:
{{ toYaml .Values.images.pullSecrets | indent 8 }}
    {{- end }}
      initContainers:
      - name: wait-webui-module
        image: {{ .Values.images.repository }}{{ .Values.images.tags.init }}
        imagePullPolicy: {{ .Values.images.pullPolicy }}
        command: ['sh', '-c', 'until nslookup webui; do echo waiting for webui; sleep 4; done;']
    {{ tuple "webui" . | include "5g-control-plane.certs_init" | nindent 6 }}
      {{ if .Values.config.coreDump.enabled }}
{{ tuple "webui" . | include "5g-control-plane.coredump_init" | nindent 6 }}
      {{ end }}
      containers:
      - name: webui
        image: {{ .Values.images.repository }}{{ .Values.images.tags.webui }}
        env:
      {{- if eq .Values.config.GIN.mode "release" }}
        - name: GIN_MODE
          value: release
      {{- end }}
        - name: SSL_CERT_FILE
          value: /var/run/certs/ca-bundle.crt
        imagePullPolicy: {{ .Values.images.pullPolicy }}
      {{ if .Values.config.coreDump.enabled }}
        securityContext:
          runAsUser: 0
      {{ end }}
        stdin: true
        tty: true
        command: ["/opt/webui-run.sh"]
      {{- if .Values.resources.enabled }}
        resources:
{{ toYaml .Values.resources.webui | indent 10 }}
      {{- end }}
        volumeMounts:
        - name: nf-config
          mountPath: /opt
        - name: certs
          mountPath: /var/run/certs
          readOnly: true
      {{- if .Values.config.coreDump.enabled }}
        - name: coredump
          mountPath: /tmp/coredump
      {{- end }}
      volumes:
      - name: nf-config
        configMap:
          name: webui
          defaultMode: 493
      - name: certs
        emptyDir: {}
      - name: certs-src
        projected:
          sources:
            - secret:
                name: webui-certs
                items:
                  - key: tls.crt
                    path: tls.crt
                  - key: tls.key
                    path: tls.key
            - secret:
                name: {{ include "5g-control-plane.shared-ca-secret-name" . }}
                items:
                  - key: ca.crt
                    path: ca.crt
    {{- if .Values.config.coreDump.enabled }}
      - name: host-rootfs
        hostPath:
          path: /
      - name: coredump
        hostPath:
          path: {{ .Values.config.coreDump.path }}
    {{- end }}
{{- end }}
